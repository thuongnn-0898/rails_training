<%= link_to gravatar_for(current_user, size: Settings.avatar_info), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("user.profile"), current_user %></span>
<span><%= t("microposts",count: current_user.microposts.count)%></span>
